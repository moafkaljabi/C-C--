#include <iostream>
using namespace std;

int max(int num1, int num2);

int result;

int main()
{
    int ret;
    int a = 100;
    int b = 200;
    int ret = max(a, b);

    return 0;
}

int max(num1, num2)
{
    if (num1 > num2)
    {
        cout << "max number is: " << result << endl;
    }
    if
        else(num2 > num1)
        {
            cout << "max number is: " << result << endl;
        }
    if
        else(num1 = num2)
        {
            cout << "equal" << endl;
        }
    else
    {
        cout << "Invalid input." << endl;
    }
    return result;
}