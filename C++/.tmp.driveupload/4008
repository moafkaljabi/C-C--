// decision making statements

/*

// if else if else

#include <iostream>
using namespace std;

int main()
{
    int a = 20;

    if (a == 30)
    {
        cout << " Value of a is = " << a << endl;
    }
    else if (a == 20)
    {
        cout << " Value of a is = " << a << endl;
    }
    else if (a == 10)
    {
        cout << " Value of a is = " << a << endl;
    }
    else
    {
        cout << " Value of a is = " << a << endl;
    }

    return 0;
}

*/

/*
// Switch Statement

#include <iostream>
using namespace std;

int main()
{
    char grade = 'B';

    switch (grade)
    {
    case 'A':
        cout << " Excellent " << endl;
        break;

    case 'B':
    case 'C':
        cout << " Good " << endl;
        break;

    case 'D':
        cout << " Pass " << endl;
        break;

    case 'F':
        cout << "Fail" << endl;
        break;

    default:
        cout << " grade invalid." << endl;
    }
    // cout << " Your Grade is: " << grade << endl;
    printf(" Your grade is: %c \n", grade);
    return 0;
}

*/

#include <iostream>
using namespace std;

int main()
{

    int a = 100;
    int b = 200;
    if (a == 100)
    {
        if (b == 200)
        {
            cout << " a = 100 & b = 200 " << endl;
        }
    }
    printf(" Exact values of a & b are: %d & %d \n", a, b);

    return 0;
}
